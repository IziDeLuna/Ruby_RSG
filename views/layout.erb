<!doctype html>
<html lang="en"">

<head>
  <title>Sentence Creator</title>
  <meta charset="utf-8">
</head>

<body>

<header>
  <h1>RubySG by Ismael Deluna</h1>
</header>

<p>Select a grammar you would like to generate:</p>

<form action="/grammar" method="POST">
  <select name="grammars">
    <% Dir.glob("grammars/*.g").each do |grammar| %>
        <option value="<%= grammar.gsub(/grammars\//,'') %>"><%= grammar.gsub(/grammars\//,'') %></option>
    <% end %>
  </select>
  <input type="submit" value="Submit" />
</form>

<section>
  <%= yield %>
</section>
</body>

</html>